if ! command -v gitleaks >/dev/null 2>&1; then
  echo "pre-commit: gitleaks is not installed, cannot scan the staged changes for secrets."
  echo "pre-commit: install it from https://github.com/gitleaks/gitleaks (winget install gitleaks)."
  exit 1
fi

gitleaks git --staged --redact --no-banner
